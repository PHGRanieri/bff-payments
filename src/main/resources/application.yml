spring:
  application:
    name: payment-service
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8081

topics:
  payment: "payment.process"

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html